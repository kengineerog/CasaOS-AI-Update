version: "3.8"

services:
  litellm:
    image: ghcr.io/berriai/litellm:main-latest
    container_name: litellm
    ports:
      - "4000:4000"
    environment:
      - LITELLM_LOG=true
    volumes:
      - litellm_config:/app
    restart: unless-stopped

volumes:
  litellm_config: